import Genoverse from './../../genoverse';
import TrackModel from './../model';

export default class ScalebarModel extends TrackModel{
  constructor(genoverse: Genoverse){
    super(genoverse);
  }
}